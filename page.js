/* 1.) Sort the array in reverse order. (Not alphabetical, but reverse
 the current Array i.e. ['a', 'c', 'b'] becomes ['b', 'c', 'a'])*/

//let testArr = ["a", "c", "b"];
//testArr.reverse();

/* 2.) Create a function that takes two numbers 
as arguments and return their sum.*/

//let add = (x,y) => x + y;

/* 3.) */
